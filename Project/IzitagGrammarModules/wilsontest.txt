iziTag {
	string text := lol
	int foo := 5
	float yas := 9.9
	int height := 500
	int width := 500
	string path := abreu.jpg
	iziImage(path, height, width)
	string par_text := Hola, como te llamas? Mucho gusto.
	string link := http://abc.go.com/
	string alias := more jews
	iziHyper(alias, link)
	iziTitle(text)
	iziSection(text)
	array blah := [hi, hola, blah]
	iziList(unordered, blah)
	iziPar(par_text)
	string id := section 2
	iziSection(id)
	iziList(unordered, blah)
	iziPar(par_text)
	}